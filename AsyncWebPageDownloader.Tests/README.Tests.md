
# AsyncWebPageDownloader - Test Project

This project contains integration test for the AsyncWebDownloader application.


## Integration Tests
- DownloaderIntegrationTests.cs  
  Tests that `Downloader.DownloadAsync()` correctly downloads and saves an HTML file from a real URL and that the saved file contains expected content then removes the file.


